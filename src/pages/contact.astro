---
import Layout from "../layouts/main.astro";
import { Button } from "../components/ui/button";
import { Card, CardContent } from "../components/ui/card";
---

<Layout
  title="Contact - TAMTAMA"
  description="Contactez l'association TAMTAMA pour vos cours de danse africaine."
>
  <!-- Hero -->
  <section
    class="py-20 bg-gradient-to-br from-accent/10 via-primary/5 to-secondary/10 relative overflow-hidden"
  >
    <div
      class="absolute top-10 right-10 w-16 h-16 rounded-full bg-primary/20 animate-float"
    >
    </div>
    <div
      class="absolute bottom-20 left-20 w-12 h-12 rounded-full bg-secondary/20 animate-float"
      style="animation-delay: 1s;"
    >
    </div>

    <div class="container relative z-10 mx-auto max-w-6xl px-4 text-center">
      <h1
        class="text-5xl font-black tracking-tight text-foreground sm:text-6xl"
      >
        Contactez-nous <span class="inline-block animate-drum">📧</span>
      </h1>
      <p
        class="mt-6 text-xl text-muted-foreground max-w-2xl mx-auto font-medium"
      >
        Une question ? Envie de réserver un spectacle ? On vous répond vite ! ⚡
      </p>
    </div>
  </section>

  <section class="py-20">
    <div class="container mx-auto max-w-6xl px-4">
      <div class="grid gap-16 lg:grid-cols-2">
        <!-- Contact Form -->
        <div>
          <h2 class="text-3xl font-black text-foreground mb-8">
            Envoyez-nous un message ✉️
          </h2>

          <form class="space-y-6" id="contact-form">
            <div class="grid gap-6 sm:grid-cols-2">
              <div>
                <label
                  for="firstName"
                  class="block text-sm font-bold text-foreground mb-2"
                >
                  Prénom *
                </label>
                <input
                  type="text"
                  id="firstName"
                  name="firstName"
                  required
                  class="w-full rounded-xl border-2 border-border bg-background px-4 py-3 text-sm text-foreground font-medium placeholder:text-muted-foreground focus:border-primary focus:outline-none"
                  placeholder="Votre prénom"
                />
              </div>
              <div>
                <label
                  for="lastName"
                  class="block text-sm font-bold text-foreground mb-2"
                >
                  Nom *
                </label>
                <input
                  type="text"
                  id="lastName"
                  name="lastName"
                  required
                  class="w-full rounded-xl border-2 border-border bg-background px-4 py-3 text-sm text-foreground font-medium placeholder:text-muted-foreground focus:border-primary focus:outline-none"
                  placeholder="Votre nom"
                />
              </div>
            </div>

            <div>
              <label
                for="email"
                class="block text-sm font-bold text-foreground mb-2"
              >
                Email *
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full rounded-xl border-2 border-border bg-background px-4 py-3 text-sm text-foreground font-medium placeholder:text-muted-foreground focus:border-primary focus:outline-none"
                placeholder="votre@email.com"
              />
            </div>

            <div>
              <label
                for="subject"
                class="block text-sm font-bold text-foreground mb-2"
              >
                Sujet *
              </label>
              <select
                id="subject"
                name="subject"
                required
                class="w-full rounded-xl border-2 border-border bg-background px-4 py-3 text-sm text-foreground font-medium focus:border-primary focus:outline-none"
              >
                <option value="">Sélectionnez un sujet</option>
                <option value="cours">💃 Inscription aux cours</option>
                <option value="essai">🎁 Cours d'essai gratuit</option>
                <option value="spectacle">🎭 Demande de spectacle</option>
                <option value="stage">🎓 Stage / Workshop</option>
                <option value="autre">❓ Autre demande</option>
              </select>
            </div>

            <div>
              <label
                for="message"
                class="block text-sm font-bold text-foreground mb-2"
              >
                Message *
              </label>
              <textarea
                id="message"
                name="message"
                required
                rows="5"
                class="w-full rounded-xl border-2 border-border bg-background px-4 py-3 text-sm text-foreground font-medium placeholder:text-muted-foreground focus:border-primary focus:outline-none resize-none"
                placeholder="Votre message..."></textarea>
            </div>

            <Button
              type="submit"
              className="w-full sm:w-auto font-bold text-base px-8 h-12"
            >
              Envoyer 🚀
            </Button>
          </form>
        </div>

        <!-- Contact Info -->
        <div class="space-y-8">
          <h2 class="text-3xl font-black text-foreground mb-8">
            Informations 📍
          </h2>

          <div class="space-y-6 stagger-children">
            {
              [
                {
                  emoji: "📍",
                  title: "Adresse",
                  content:
                    "Studio TAMTAMA\n42 Rue de la Danse\n75011 Paris, France",
                },
                {
                  emoji: "📧",
                  title: "Email",
                  content: "contact@tamtama.fr",
                  isLink: true,
                },
                { emoji: "📞", title: "Téléphone", content: "01 23 45 67 89" },
                {
                  emoji: "🕐",
                  title: "Horaires d'accueil",
                  content:
                    "Lundi - Vendredi : 17h - 21h\nSamedi : 10h - 18h\nDimanche : Fermé",
                },
              ].map((item) => (
                <Card className="border-2 bounce-click">
                  <CardContent className="flex items-start gap-4 pt-6">
                    <div class="flex-shrink-0 w-14 h-14 rounded-xl bg-gradient-to-br from-primary/10 to-secondary/10 flex items-center justify-center text-2xl border-2 border-border">
                      {item.emoji}
                    </div>
                    <div>
                      <h3 class="font-bold text-foreground">{item.title}</h3>
                      {item.isLink ? (
                        <a
                          href={`mailto:${item.content}`}
                          class="text-primary font-medium hover:underline"
                        >
                          {item.content}
                        </a>
                      ) : (
                        <p class="text-muted-foreground font-medium whitespace-pre-line">
                          {item.content}
                        </p>
                      )}
                    </div>
                  </CardContent>
                </Card>
              ))
            }
          </div>

          <!-- Social -->
          <div class="pt-4">
            <h3 class="font-bold text-foreground mb-4">Suivez-nous ! 📱</h3>
            <div class="flex gap-3">
              {
                ["👍", "📸", "🎬"].map((emoji, i) => (
                  <a
                    href="#"
                    class="w-12 h-12 rounded-xl border-2 border-border bg-background flex items-center justify-center text-xl hover:border-primary hover:bg-primary/5 transition-all bounce-click"
                  >
                    {emoji}
                  </a>
                ))
              }
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>
